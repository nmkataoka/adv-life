import { NextYearButton } from './NextYearButton';

/** This map overlay should contain UI elements specific to civ mode. */
export function CivMapOverlay(): JSX.Element {
  return (
    <>
      <NextYearButton />
    </>
  );
}

import React from 'react';
import styled from '@emotion/styled';

export default function WorldMap(): JSX.Element {
  return <Container>World Map</Container>;
}

const Container = styled.div`
  min-width: 500px;
  min-height: 300px;
`;
